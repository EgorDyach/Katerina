import Text from "../../../components/Text.jsx";

function Logo({src}) {
    return (
        // <img className={"header__logo"} alt={""} src={src} />
        <Text className={"header__logo"} As={"p"} size={40} color={"#000"}>ЛОГО</Text>
    )
}

export default Logo;